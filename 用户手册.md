# Codex API Manager — 用户手册

> 版本：一期（2026-08-18）

## 一、这是什么

Codex（OpenAI 的命令行/桌面编程工具）通过 `~/.codex/config.toml` 配置文件切换 API 供应商。本工具让你**不用手改配置文件**，用图形界面完成：

- 一键切换供应商（选模板 → 选模型 → 填 API key → 应用）
- 测试 API 是否连通
- 管理 API key（Windows 用户环境变量 / .env）
- 一键备份、恢复、回到默认状态

## 二、安装与启动

### 方式一：绿色版 exe（推荐）

1. 双击 `dist\CodexAPIManager.exe` 即可打开桌面窗口
2. 无需安装 Python 或其他环境

### 方式二：源码运行

```bash
pip install -r requirements.txt
python app.py            # 桌面窗口
python app.py --browser  # 用浏览器打开
```

> 桌面窗口用的是系统自带 WebView2 引擎（Win10/11 自带）。若窗口空白，检查 Windows 更新中的 Edge WebView2 运行时。

## 三、界面说明（6 个页面）

顶部标签页依次为：

### 1. 快速切换（最常用）

一键切换供应商的入口：

1. 点一个**供应商卡片**（OpenAI 官方 / Azure OpenAI / OpenRouter 聚合 / Groq / xAI Grok / Perplexity / Hugging Face / Ollama / LM Studio / 通用 Responses 中转站）
2. 选**模型**（下拉可输入自定义）
3. 填 **API Key**（本地模型如 Ollama 无需填）
4. 点「**应用并切换**」

工具会自动完成：写 `config.toml` → 存 key 到环境变量 → 自动备份 → 显示结果。

> 卡片右上角出现绿色「当前」标记 = 该供应商正在生效。
>
> ⚠️ 写入用户环境变量后，**需要重启 Codex Desktop** 才会生效。

### 2. Provider 管理

进阶功能，直接查看和编辑所有 provider：

- **Provider 列表**：查看/编辑/删除每个 provider；「生效中」标记当前使用的
- **新增 Provider**：手填完整字段（基础 + 高级选项：query_params / headers 等）
- **全局设置**：model、model_provider、openai_base_url、oss_provider

### 3. 连接测试

验证 API 地址和 key 是否可用：

- 填 base_url、模型、key → 点「运行测试」
- 判定标准：发一个最小 `/responses` 请求（这是硬条件，成功才算连通）
- 附带探测 `/models` 接口列出可用模型（失败不影响判定）

### 4. 环境变量

管理 API key 的存放位置：

- **用户环境变量**（左）：Codex Desktop 能读到的，推荐在这里存 key
- **.env 文件**（右）：仅供自定义脚本用，Desktop 不读
- 按钮可「从 .env 导入到用户环境变量」或反向导出
- 所有 key 默认遮蔽显示（`sk****abcd`），点「显示」可看明文

### 5. 备份与恢复

- **创建备份**：手动存档当前配置
- **备份列表**：每次工具写配置前都会自动备份（来源标记：手动/自动/恢复前），任意时间点可一键恢复
- **恢复默认**：不是把整个配置文件还原成出厂状态，而是**只移除本工具改过的设置**（弹窗列出清单，可勾选），插件、MCP 等其它配置完全不受影响
- **我的默认**：把当前 API 配置另存为一个模板，以后一键应用回来

### 6. 命令中心

1.1 版本提供（codex doctor 等命令图形化执行），当前为占位页。

## 四、关键概念

| 概念 | 说明 |
|---|---|
| provider | 一个 API 供应商条目，含名称、地址（base_url）、key 来源（env_key） |
| env_key | 存放 key 的环境变量名，如 `OPENAI_API_KEY` |
| wire_api | 协议类型。新版 Codex **只支持 `responses`**，旧的 `chat` 已移除 |
| 覆盖追踪 | 工具记录自己改过哪些键（原值是什么），用于"恢复默认" |

## 五、常见问题

**Q：切换后 Codex 没反应？**
写入 key 到用户环境变量后需要**重启 Codex Desktop**（GUI 程序只在启动时读取环境变量）。

**Q：`wire_api` 填 chat 报错？**
新版 Codex 已移除 chat 协议。本工具内置的 10 个模板全部原生支持 Responses 协议。如果你的供应商只提供 Chat Completions 接口（多数国内官方 API，如 DeepSeek/通义/豆包/Kimi/智谱），需要经一个兼容 Responses 的中转站。本工具二期将内置自动转换代理，届时这些服务商也能一键接入。

**Q：填了自定义地址但测试失败？**
- 确认 base_url 以 `/v1` 结尾（如 `https://api.example.com/v1`）
- 确认 key 正确且权限足够
- 在「连接测试」页运行测试看具体错误（401=key 无效，404=地址不对，429=限流）

**Q：恢复默认会删掉我的插件配置吗？**
不会。「恢复默认」只移除本工具写过的键（弹窗里会列出清单供你确认），其余配置原样保留。

**Q：备份存在哪里？**
`~/.codex/codex-api-manager/backups/`，每次写操作前自动生成一份。

**Q：工具修改会损坏 config.toml 吗？**
不会。工具用增量方式写入，只改 API 相关字段，且写前自动备份。即使出问题也可随时恢复。

## 六、注意事项

- 本工具只管理 API 相关配置；不会触碰你的聊天记录、插件、MCP 服务器设置
- API key 只保存在你的电脑上（用户环境变量或 .env），工具不会上传任何数据
- 建议更换 key 时在环境变量页删除旧 key，避免残留
